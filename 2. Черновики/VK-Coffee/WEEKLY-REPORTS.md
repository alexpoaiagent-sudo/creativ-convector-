---
project: VK-Coffee
role: F5
status: черновик
created: 2026-01-26
updated: 2026-01-26
tags: ["отчет", "cron", "node"]
description: # Настройка автоматических еженедельных отчетов  ## Обзор  Система автоматически генерирует еженедел...
original_file: WEEKLY-REPORTS.md
---

# Настройка автоматических еженедельных отчетов

## Обзор

Система автоматически генерирует еженедельные отчеты о работе с заметками:
- Сколько заметок поступило
- Куда они были распределены
- Для каких веток ВК кофе подходят
- Статистика по проектам

---

## Ручной запуск

### Генерация отчета

```bash
cd "/Users/alexander/Documents/Заметки"
npm run weekly
```

### Автоматизация с Git

```bash
./scripts/auto-weekly-report.sh
```

Этот скрипт:
1. Генерирует отчет
2. Фиксирует изменения в Git
3. Отправляет на GitHub
4. Показывает статистику

---

## Автоматический запуск через cron

### Шаг 1: Сделать скрипт исполняемым

```bash
chmod +x "/Users/alexander/Documents/Заметки/scripts/auto-weekly-report.sh"
```

### Шаг 2: Открыть crontab

```bash
crontab -e
```

### Шаг 3: Добавить задачу

Выберите один из вариантов:

#### Вариант 1: Каждое воскресенье в 20:00

```cron
0 20 * * 0 /Users/alexander/Documents/Заметки/scripts/auto-weekly-report.sh >> /Users/alexander/Documents/Заметки/logs/weekly-report.log 2>&1
```

#### Вариант 2: Каждую пятницу в 18:00

```cron
0 18 * * 5 /Users/alexander/Documents/Заметки/scripts/auto-weekly-report.sh >> /Users/alexander/Documents/Заметки/logs/weekly-report.log 2>&1
```

#### Вариант 3: Каждый понедельник в 9:00

```cron
0 9 * * 1 /Users/alexander/Documents/Заметки/scripts/auto-weekly-report.sh >> /Users/alexander/Documents/Заметки/logs/weekly-report.log 2>&1
```

### Шаг 4: Сохранить и выйти

В редакторе vim:
1. Нажмите `i` для режима вставки
2. Вставьте строку cron
3. Нажмите `Esc`
4. Введите `:wq` и нажмите `Enter`

### Шаг 5: Проверить установку

```bash
crontab -l
```

Вы должны увидеть добавленную задачу.

---

## Формат cron

```
┌───────────── минута (0 - 59)
│ ┌───────────── час (0 - 23)
│ │ ┌───────────── день месяца (1 - 31)
│ │ │ ┌───────────── месяц (1 - 12)
│ │ │ │ ┌───────────── день недели (0 - 7) (0 и 7 = воскресенье)
│ │ │ │ │
* * * * * команда
```

### Примеры

```cron
# Каждый день в 9:00
0 9 * * * /path/to/script.sh

# Каждый понедельник в 8:30
30 8 * * 1 /path/to/script.sh

# Каждое 1-е число месяца в 00:00
0 0 1 * * /path/to/script.sh

# Каждые 6 часов
0 */6 * * * /path/to/script.sh
```

---

## Логи

### Создать директорию для логов

```bash
mkdir -p "/Users/alexander/Documents/Заметки/logs"
```

### Просмотр логов

```bash
# Последние 50 строк
tail -50 "/Users/alexander/Documents/Заметки/logs/weekly-report.log"

# Следить за логами в реальном времени
tail -f "/Users/alexander/Documents/Заметки/logs/weekly-report.log"

# Весь лог
cat "/Users/alexander/Documents/Заметки/logs/weekly-report.log"
```

### Очистка старых логов

```bash
# Удалить логи старше 30 дней
find "/Users/alexander/Documents/Заметки/logs" -name "*.log" -mtime +30 -delete
```

---

## Структура отчетов

### Расположение

```
/Users/alexander/Documents/Заметки/reports/weekly/
├── week-1-2026-01-05.md
├── week-2-2026-01-12.md
├── week-3-2026-01-19.md
└── latest.md  ← Всегда последний отчет
```

### Содержимое отчета

```markdown
# Еженедельный отчет

**Период:** 5 января — 12 января
**Неделя:** 3

## 📈 Общая статистика
- Новых заметок: 5
- Изменено заметок: 3
- Удалено заметок: 1

## 📁 Детальная статистика по проектам
### БК - Кофейня
- Новые заметки (3):
  - Рецепт латте.md
  - Обучение бариста.md
  - Техника эспрессо.md

## 🏢 Соответствие веткам ВК кофе
### ☕ Рецепты и техники
- Рецепт латте.md
- Техника эспрессо.md

### 🎓 Обучение и тренинги
- Обучение бариста.md
```

---

## Troubleshooting

### Проблема: cron не запускается

**Решение 1: Проверить права**
```bash
chmod +x "/Users/alexander/Documents/Заметки/scripts/auto-weekly-report.sh"
```

**Решение 2: Проверить путь**
```bash
which node
# Добавьте полный путь к node в скрипт
```

**Решение 3: Проверить cron**
```bash
# Проверить, что cron работает
sudo launchctl list | grep cron

# Перезапустить cron (macOS)
sudo launchctl stop com.vix.cron
sudo launchctl start com.vix.cron
```

### Проблема: Скрипт не находит npm

**Решение:**

Добавьте в начало скрипта `auto-weekly-report.sh`:

```bash
# Добавить путь к node и npm
export PATH="/usr/local/bin:/usr/bin:/bin:$HOME/.nvm/versions/node/v20.0.0/bin:$PATH"
```

Замените версию node на вашу:
```bash
node --version
which node
```

### Проблема: Нет прав на запись логов

**Решение:**
```bash
mkdir -p "/Users/alexander/Documents/Заметки/logs"
chmod 755 "/Users/alexander/Documents/Заметки/logs"
```

### Проблема: Git не может сделать push

**Решение:**

Проверьте SSH ключи:
```bash
ssh -T git@github.com
```

Если ошибка, добавьте ключ:
```bash
ssh-add ~/.ssh/id_ed25519
```

---

## Альтернатива: launchd (macOS)

Для macOS рекомендуется использовать launchd вместо cron.

### Создать plist файл

```bash
nano ~/Library/LaunchAgents/com.notes.weekly-report.plist
```

### Содержимое файла

```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>com.notes.weekly-report</string>

    <key>ProgramArguments</key>
    <array>
        <string>/Users/alexander/Documents/Заметки/scripts/auto-weekly-report.sh</string>
    </array>

    <key>StartCalendarInterval</key>
    <dict>
        <key>Weekday</key>
        <integer>0</integer>  <!-- 0 = Воскресенье -->
        <key>Hour</key>
        <integer>20</integer>
        <key>Minute</key>
        <integer>0</integer>
    </dict>

    <key>StandardOutPath</key>
    <string>/Users/alexander/Documents/Заметки/logs/weekly-report.log</string>

    <key>StandardErrorPath</key>
    <string>/Users/alexander/Documents/Заметки/logs/weekly-report-error.log</string>
</dict>
</plist>
```

### Загрузить задачу

```bash
launchctl load ~/Library/LaunchAgents/com.notes.weekly-report.plist
```

### Проверить статус

```bash
launchctl list | grep weekly-report
```

### Выгрузить задачу

```bash
launchctl unload ~/Library/LaunchAgents/com.notes.weekly-report.plist
```

---

## Уведомления

### Добавить уведомление в скрипт

Добавьте в конец `auto-weekly-report.sh`:

```bash
# Отправить уведомление (macOS)
osascript -e 'display notification "Еженедельный отчет создан" with title "Notes Manager"'
```

### Отправка на email (опционально)

Установите `mailutils`:
```bash
brew install mailutils
```

Добавьте в скрипт:
```bash
# Отправить отчет на email
cat "$LATEST_REPORT" | mail -s "Еженедельный отчет" your@email.com
```

---

## Рекомендации

1. **Выберите удобное время** - когда вы обычно не работаете
2. **Проверяйте логи** - раз в месяц просматривайте логи
3. **Резервное копирование** - отчеты автоматически сохраняются в Git
4. **Настройте уведомления** - чтобы знать, что отчет создан

---

## Команды для управления

```bash
# Запустить вручную
npm run weekly

# Запустить с автоматизацией
./scripts/auto-weekly-report.sh

# Посмотреть последний отчет
cat reports/weekly/latest.md

# Посмотреть все отчеты
ls -lh reports/weekly/

# Удалить старые отчеты (старше 3 месяцев)
find reports/weekly/ -name "week-*.md" -mtime +90 -delete
```

---

**Дата создания:** 2026-01-12
**Автор:** AI Agent + Claude Sonnet 4.5
